cluster/vpn
cluster/democratic-csi
router/ansible/backups/
router/ansible/ansible.log
# Track shareable Claude Code config (skills, project settings, docs) but keep
# personal/local overrides out of git.
.claude/*
!.claude/skills/
!.claude/settings.json
!.claude/MCP.md
.claude/settings.local.json

# Terraform
.terraform/
*.tfstate
*.tfstate.*

# Ad-hoc credential drops (Google OAuth client etc). Real secrets belong in
# *.secret.yaml (git-crypt) and are sealed into the cluster by ./sign.sh.
galaxy-sso.env
*.env
