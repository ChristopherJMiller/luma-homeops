---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-pls-redis
  namespace: nexus-pls
  labels:
    app: nexus-pls-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nexus-pls-redis
  template:
    metadata:
      labels:
        app: nexus-pls-redis
    spec:
      containers:
        - name: nexus-pls-redis
          image: docker.io/redis:7-alpine@sha256:2700d5097763fda285c463f4eefc3d0730a2df2a9d48e66707b19d5a5e5f23d4
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: nexus-pls-redis-data
---
apiVersion: v1
kind: Service
metadata:
  name: nexus-pls-redis
  namespace: nexus-pls
spec:
  selector:
    app: nexus-pls-redis
  ports:
    - protocol: TCP
      port: 6379
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-pls-redis-data
  namespace: nexus-pls
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 4Gi
