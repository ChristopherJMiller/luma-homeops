---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: id
  labels:
    app: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: docker.io/redis:7-alpine@sha256:8609d315f7fa6837fddf9a7bb1ad614ffc1eec7f7b516e94a1aa35219fc29a36
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: redis
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: id
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis
  namespace: id
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-fs-hdd
  resources:
    requests:
      storage: 8Gi
