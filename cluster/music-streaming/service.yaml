apiVersion: v1
kind: Service
metadata:
  name: gonic
  namespace: music-streaming
  labels:
    app: gonic
spec:
  selector:
    app: gonic
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
