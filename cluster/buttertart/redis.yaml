---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: buttertart-redis
  namespace: buttertart
  labels:
    app: buttertart-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: buttertart-redis
  template:
    metadata:
      labels:
        app: buttertart-redis
    spec:
      containers:
        - name: buttertart-redis
          image: docker.io/redis:7-alpine@sha256:ef3296cb1b3a7eb40f2a992a398777a1c0b5b21df44f1a5bef84067f772daf54
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: buttertart-redis-data
---
apiVersion: v1
kind: Service
metadata:
  name: buttertart-redis
  namespace: buttertart
spec:
  selector:
    app: buttertart-redis
  ports:
    - protocol: TCP
      port: 6379
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: buttertart-redis-data
  namespace: buttertart
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: freenas-nfs-csi
  resources:
    requests:
      storage: 2Gi
