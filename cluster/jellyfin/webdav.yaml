---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jellyfin-webdav
  namespace: jellyfin
  labels:
    app: jellyfin-webdav
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jellyfin-webdav
  template:
    metadata:
      labels:
        app: jellyfin-webdav
    spec:
      containers:
        - name: jellyfin-webdav
          image: docker.io/apachewebdav/apachewebdav:latest@sha256:0d4aea1eefbc374cf21fd677e09a90df7dca0f5b5d72e5ba534f201f7c03bcea
          ports:
            - containerPort: 80
          env:
            - name: AUTH_TYPE
              valueFrom:
                secretKeyRef:
                  name: jellyfin-webdav-creds
                  key: AUTH_TYPE
            - name: USERNAME
              valueFrom:
                secretKeyRef:
                  name: jellyfin-webdav-creds
                  key: USERNAME
            - name: PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jellyfin-webdav-creds
                  key: PASSWORD
            - name: LOCATION
              value: /
            - name: PUID
              value: '82'
            - name: GUID
              value: '82'
          volumeMounts:
            - name: data
              mountPath: /var/lib/dav/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: jellyfin-media
---
apiVersion: v1
kind: Service
metadata:
  name: jellyfin-webdav
  namespace: jellyfin
spec:
  selector:
    app: jellyfin-webdav
  ports:
    - protocol: TCP
      port: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jellyfin-webdav
  namespace: jellyfin
  annotations:
    external-dns.alpha.kubernetes.io/target: luma.chrismiller.xyz
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-buffering: off
    nginx.ingress.kubernetes.io/http2_max_concurrent_streams: 256
    nginx.ingress.kubernetes.io/use-gzip: true
spec:
  tls:
    - hosts:
        - dav.nas.luma.chrismiller.xyz
      secretName: dav-nas-luma-tls
  rules:
    - host: dav.nas.luma.chrismiller.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jellyfin-webdav
                port:
                  number: 80
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dav-nas-luma-tls
  namespace: jellyfin
spec:
  secretName: dav-nas-luma-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  commonName: dav.nas.luma.chrismiller.xyz
  dnsNames:
    - dav.nas.luma.chrismiller.xyz
  privateKey:
    rotationPolicy: Always
    algorithm: ECDSA
    size: 384
