---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recipes-pvc
  namespace: recipes
spec:
  storageClassName: freenas-nfs-ssd-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
