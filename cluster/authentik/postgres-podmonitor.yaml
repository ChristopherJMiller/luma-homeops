---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgres-acid-auth
  namespace: authentik
  labels:
    app: postgresql
    cluster: acid-auth
    release: prometheus
spec:
  selector:
    matchLabels:
      application: spilo
      galaxy: acid-auth
  podMetricsEndpoints:
    - port: metrics
      interval: 30s
      path: /metrics
      honorLabels: true
  namespaceSelector:
    matchNames:
      - authentik