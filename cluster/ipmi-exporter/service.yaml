---
apiVersion: v1
kind: Service
metadata:
  name: ipmi-exporter
  namespace: prometheus
  labels:
    app: ipmi-exporter
spec:
  selector:
    app: ipmi-exporter
  ports:
    - name: http
      port: 9290
      targetPort: http
      protocol: TCP
