---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nextcloud
  namespace: nextcloud
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - path: /nextcloud
            pathType: Prefix
            backend:
              service:
                name: nextcloud
                port:
                  number: 8080
